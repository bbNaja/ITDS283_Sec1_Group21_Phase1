class Profile {
  String? email;
  String? password;
  String? name;
  int? age;
  // String? place;
//add , this.name, this.age, this.place later
  Profile({this.email, this.password, this.name, this.age});
}
